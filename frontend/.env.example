# Local dev: leave unset — Vite proxies /api to http://localhost:8000
# Production (Vercel): set this to your deployed backend URL, no trailing slash
# VITE_API_URL=https://ctrl-interview-backend.onrender.com
